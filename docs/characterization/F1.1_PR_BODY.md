## Summary

Establishes the F1.1 Legacy runtime characterization baseline from repository evidence and supplies the canonical manual Unity run record. Production runtime code and assets are unchanged.

## Static evidence

- Verified starting `develop` commit: `2e2d5e3baf1adc0990c3da8b0dcc25e33b907db2`.
- Verified Unity `6000.3.13f1` and the three enabled production scenes from project settings.
- Documented exact source/assets in `docs/characterization/F1.1_LEGACY_RUNTIME_STATIC_MAP.md`.

## Runtime map

STATIC VERIFIED: Boot-hosted MainMenu -> OpenWorld -> Duel -> OpenWorld/MainMenu, including exact scene/controller/builder/bootstrap dependencies.

## Lifetime map

STATIC VERIFIED: existing DDOL calls, singleton patterns, mutable static state, runtime roots, EventSystem, Camera, and AudioListener creation/ownership paths.

## Duel transition map

STATIC VERIFIED: normal OpenWorld writer, session/legacy state fields, Duel bootstrap and readers, result/progression/save writers, and return path. The observed runtime values remain NOT TESTED.

## Tests

No runtime tests added. This is a documentation-only characterization slice; repository static inspection and Unity compilation are the applicable automated checks.

## Automated validation

- Unity 6000.3.13f1 compilation: BLOCKED before compilation by missing `com.unity.editor.headless` entitlement (Unity exit code 198; no compile result claimed)
- EditMode tests: BLOCKED by the same Unity license condition; repository inspection found no existing test assemblies
- Generated `Assembly-CSharp.csproj` fallback: BLOCKED because Unity did not generate `Temp/obj/Assembly-CSharp/project.assets.json`; this is not counted as a compile result
- YAML template structural checks: PASS (26 uniquely numbered canonical steps; every initial status is `NOT_TESTED`; status vocabulary is declared)
- `git diff --check`: PASS
- Final diff/scope audit: PASS
- ProjectSettings/Packages unchanged: PASS

## Manual validation required

MANUAL VALIDATION REQUIRED. Run all 26 steps in `docs/characterization/F1.1_LEGACY_RUNTIME_BASELINE.yaml` with Unity 6000.3.13f1. All statuses intentionally begin as `NOT_TESTED`.

## Known limitations

- No Play Mode result is claimed.
- Static inspection cannot establish active runtime counts, lifecycle ordering, Console output, input success, AI action, save-file effects, or object accumulation.

## Out of scope

Camera/AudioListener fixes, replacement of Duel DebugBootstrap, DuelContext/DuelResult, game-state decomposition, scene/controller/builder refactors, gameplay/rules/AI/progression/save changes, migrations, upgrades, Steam/TBD work, and unrelated cleanup.

## Deviations

Automated Unity compilation and EditMode execution could not run because the installed matching editor lacks a valid headless entitlement. Manual owner compilation is therefore still required. No implementation-scope deviation.
